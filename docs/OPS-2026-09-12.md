# 运维与项目进度记录 — 2026-09-12

> 记录范围：09-11 深夜 ~ 09-12 凌晨的动作（MinIO 临时对象存储上线、无限画布内置渠道清理、服务器与本地清理）。
> 环境：生产服务器（`ai.1canc.com` 实例，宝塔 nginx 为唯一公网入口）。

## 一、当前运行拓扑（本时段收尾状态）

| 服务 | 镜像 / 形态 | 监听 | 状态 |
|---|---|---|---|
| sub2api 主站 | `sub2api:local-20260911-2`（= `sub2api:latest`） | 127.0.0.1:8080 | healthy |
| 对话工作台 lobehub | `lobe-chat:bridge-sub2api-v2` | 127.0.0.1:3210 | Up |
| 生图工作台 image-app | `nginx:1.27-alpine` + `/var/www/image-app` | 127.0.0.1:3002 | Up |
| 无限画布 canvas | 静态站 `/www/wwwroot/canvas.ai.1canc.com`（entry `index-gwWxJcpJ.js`） | 宝塔静态目录 | Up |
| **MinIO（本时段新增）** | `minio/minio:latest`（RELEASE.2025-09-07） | 127.0.0.1:9000（API）/ 9001（Console） | Up |
| 数据库 | sub2api-postgres / sub2api-redis / lobehub-postgres | 仅内部网络 | healthy |

域名：主站 `ai.1canc.com`；工作台 `chat / draw / canvas .ai.1canc.com`（宝塔反代/静态站）；
**对象存储 `s3.ai.1canc.com`（本时段新增，宝塔反代 → `http://127.0.0.1:9000`，独立 Let's Encrypt 证书）**。

## 二、本时段变更

### 1. MinIO 临时对象存储上线（对话工作台上传功能修复）

**背景**：lobehub 的 S3 原为跳过启动校验的假配置（`https://s3.local`），任何文件上传均 DNS 解析失败。

**部署**（全部在服务器侧，主 compose 未改动）：
- 容器 `minio`：API `127.0.0.1:9000`、Console `127.0.0.1:9001`，卷 `playground_minio_data`，网络 `playground`
- 配置写入 `/opt/playground/docker-compose.override.yml`（新增 minio 服务 + lobehub 的 S3 env 与 `extra_hosts`）
- 凭证写入 `/opt/playground/.env`（`MINIO_ROOT_USER=lobe-admin`；S3 专用账号 `lobe-uploader`）
- Bucket `lobe-temp`：匿名 download 可读；账号策略 `lobe-temp-rw`（仅限该桶）；**ILM 生命周期规则 2 天自动过期**（rule `dai1pft1dnls7cht836g`）
- lobehub env：`S3_ENDPOINT=https://s3.ai.1canc.com`、`S3_PUBLIC_DOMAIN` 同、`S3_BUCKET=lobe-temp`、`S3_ENABLE_PATH_STYLE=1`、`S3_SET_ACL=`（空）、`extra_hosts: s3.ai.1canc.com → 172.20.0.1`（容器走宿主机 nginx）

**排障过程（三个关键坑，务必留档）**：

1. **`S3_ENABLE_PATH_STYLE` 必须写 `1`**
   lobe 源码判断为 `process.env.S3_ENABLE_PATH_STYLE === '1'`，写 `true` 会被判为 false，
   SDK 将走 virtual-host 风格（`bucket.s3.domain`）导致证书不匹配/404。

2. **宝塔全局 `proxy.conf` 的 `proxy_cache cache_one` 会把 HEAD 改写成 GET**
   宝塔 `/www/server/nginx/conf/proxy.conf` 含 `proxy_cache cache_one;`（http 级生效），
   配合 nginx 默认 `proxy_cache_convert_head on` → 经反代的 **HEAD 请求被改写为 GET 发往上游**，
   S3 预签名（按 HEAD 签名）校验必然失败（403），导致 lobe 上传流程的 `HeadObject` 预检查中断。
   **修复**：在 `s3.ai.1canc.com.conf` 的 location 内追加
   `proxy_cache off;` 与 `proxy_cache_convert_head off;`（改前备份 `*.bak-minio-fix-20260911-234114`，`nginx -t` 后平滑 reload）。

3. **`S3_SET_ACL` 保持空（false）**
   MinIO 不支持对象 ACL 头；文件预览由 lobe 走 2 小时期预签名 URL（`getFullFileUrl` 行为）。

**验证（端到端，浏览器真实上传）**：`HEAD(404 预检) → OPTIONS 204 → PUT 200 → HEAD 200 → GET 200` 全链路通过；
对象落盘 `lobe-temp/files/496984/fb0c406a-*.txt`；非法/过期签名与匿名 PUT 均正确拒绝（403）。

### 2. 无限画布「默认渠道」清理（画布非工作台模型混入修复）

**现象**：画布模型选择器出现 `gpt-image-2(默认渠道)`，非工作台配置项。

**根因**：画布 `web/src/stores/use-config-store.ts` 的 `defaultConfig` 内置一个 `id="default"` 的示例渠道
（含 gpt-image-2 / grok-imagine-video / gpt-5.5 / gpt-4o-mini-tts 四个示例模型）；
sub2api 注入桥只「新增/更新」渠道、从不清理它 → 示例模型与工作台模型混显。

**修复**（infinite-canvas 仓库 commit `c8c42b1`，+22 行）：
- `web/src/lib/sub2api-bridge.ts` 新增 `withoutBuiltinDefaultChannel()` / `dropBuiltinDefaultChannel()`：
  **工作台注入到达时（幂等指纹检查之前）**清理 `id === "default" && !apiKey` 的占位渠道（已手动配置 key 的不会被误删）
- 本地 `npm run build`（vite）→ tar 上传 → 部署 `/www/wwwroot/canvas.ai.1canc.com`
  （旧站备份 `/root/canvas-backup/canvas-site-20260912-000130.tar.gz`）
- 新入口 `assets/index-gwWxJcpJ.js`；画布站点为纯静态站，`infinite-canvas` 容器未改动

**验证**：清空浏览器数据 → 从工作台进入画布 → localStorage 与配置面板均仅剩
「ZN-GPT福利组」「C2-Image-2.5」两个渠道 ✅（老脏数据在下次注入时同样被自动清理）。

### 3. 服务器与本地资源清理

**服务器（回收约 600MB）**：
- `/tmp`：`sub2api-image-v2.tar`(137M)、`out_*.tgz`、`canvas-dist.tar.gz`、mctrace 等 → 144M → 108K
- 未使用镜像：`node:24-slim`(332M)、`busybox`、`alpine:3.21`（`docker rmi`）
- 旧构建二进制 `/opt/sub2api-build/sub2api`（117M，保留 Dockerfile/entrypoint 脚手架）
- nginx 大日志就地清空（chat 11M、ai 2.7M 等，共约 14M）
- **保留**：`sub2api:latest` + `local-20260911-1703`（同 ID，回滚用）、`minio/mc`、
  `/opt/playground/src`（lobe 源码，待后续补丁重建用）、`/www/backup`（宝塔备份）

**本地（E:/github/sub2AI，回收约 1.3GB）**：
- sub2api 仓库根目录：16 张验证截图、probe/deploy yaml、B_lobe 调试脚本、`sub2api-image.tar`(139.5M)、
  `.playwright-cli/` 缓存 → 清理（关键 2 张截图与 2 个脚本归档至 WorkBuddy `.workbuddy/archive/20260912/`）
- 工作区根：`lobe-chat-v2.tar`(1051M)、`sub2api-image-v2.tar`(136M)、4 个构建日志 → 删除
  （均为可再生的构建中间产物；服务器已存有源码包）
- 清空 `backend/internal/web/dist` 历史构建残留（181 files → 0；该目录未被 git 跟踪，为「多套残留」问题源，已按 09-11 记录的建议处理）

## 三、验证记录（全部通过）

- 五个站点：主站 200 / chat 302 / draw 200 / canvas 200 / s3 health 200
- 对话上传端到端（含 CORS/预签名/落盘/2 天过期标注）通过
- 画布渠道注入与清理逻辑通过（UI 与 localStorage 双确认）
- **lobe 内置模型隐藏补丁复核**：`lobe-chat:bridge-sub2api-v2` 镜像内包含 `batchToggleAiModels` 逻辑；
  数据库 `ai_models` 中 deepseek 内置模型（`deepseek-v4-flash / v4-pro / v4-flash-vision-exp`）`enabled=false`，
  即 09-11 记录的补丁**已部署生效**（此前“未部署”的判断为过时信息，特此更正）
- 服务器容器 8 个全部 Up；无 Exited 容器残留；磁盘 21G / 79G（28%）

## 四、待办 / 注意事项

1. **s3 反代站点的通用坑**：宝塔默认反代受全局 `proxy_cache cache_one` 影响会把 HEAD 改写为 GET；
   今后接入任何 **S3/签名类服务**的反代，都需在站点 location 内加 `proxy_cache off; proxy_cache_convert_head off;`
2. `s3.ai.1canc.com` 站点未单独设置 `client_max_body_size`（当前继承全局 50m），如需更大附件再调
3. MinIO 临时文件策略为 **2 天自动过期**；如业务需要可在 `mc ilm rule` 调整
4. 无限画布的 `infinite-canvas` 容器（127.0.0.1:3000）仍为旧产物副本，仅作留档；公网访问走宝塔静态站
5. 三个仓库（sub2api / lobe-chat / infinite-canvas / gpt_image_playground）本地提交均未推送远端

## 五、回滚说明

- **MinIO**：还原 `/opt/playground/docker-compose.override.yml` 对应备份 + `docker compose up -d lobehub`（S3 回到假配置，上传不可用）
- **画布**：`/root/canvas-backup/canvas-site-20260912-000130.tar.gz` 解包覆盖 `/www/wwwroot/canvas.ai.1canc.com`
- **s3 nginx**：`/www/server/panel/vhost/nginx/s3.ai.1canc.com.conf.bak-minio-fix-*` 还原后 `nginx -s reload`
- **sub2api 主站**：`/root/sub2api-deploy/` 下 compose 备份切换 image 行（`.bak-pre-switch-20260911` 等）

---

## 附：对话工作台性能优化专项（09-12 凌晨，A+B+D）

**背景**：对话工作台（lobe）加载慢、切换路由后整页重载。

**实测（优化前，真实浏览器 + 本地网络）**：
- 首屏 DOMContentLoaded **17.9s**（250 请求 / 4.06MB gzip 后）
- **二次访问 5.3s，仍传输 3.44MB（250 请求仅 11 个命中缓存）** ← 核心问题
- 服务端无瓶颈：TTFB 164ms、容器空闲、出口 ~19Mbps；实测用户下载 251-472KB/s（~2-3.8Mbps）
- 服务器侧 BBR / HTTP/3(QUIC) / gzip / gzip_proxied 均已启用

**根因**：
1. lobe（Next.js public 目录）静态资源 `Cache-Control: public, max-age=0` → 每次访问重复下载、无强缓存
2. 首屏 4MB / 250 请求 在 ~2Mbps 链路上理论下限即 16s
3. 壳页面路由切换销毁 iframe → 第三方应用（lobe）整页重载

**已实施优化**：
- **A. 静态资源长缓存**：`chat.ai.1canc.com.conf` 新增 `location ^~ /_spa/assets/`
  （`proxy_hide_header Cache-Control/ETag` + `add_header Cache-Control "public, max-age=31536000, immutable"` + `gzip_proxied any`）
  备份：`/root/perf-backup/chat.ai.1canc.com.conf.bak-20260912-003626`
- **B. 工作台 iframe 保活（keep-alive）**：`frontend/src/App.vue` 的 RouterView 改为
  `<KeepAlive :include="['PlaygroundView']">` + `<component :key="route.fullPath">`；
  `PlaygroundView.vue` 增加 `defineOptions({ name: 'PlaygroundView' })` 与 `onActivated` 幂等重注入
  （管理后台配置变更后，切回时自动重新注入）。
  提交 `aa744557f`；镜像 `sub2api:perf-20260912`（新入口 `assets/index-CFqgCMsf.js`）
- **D. gzip 压缩级别**：全局 `gzip_comp_level 2 → 6`（备份 `/root/perf-backup/nginx.conf.bak-20260912-003626`）；
  Brotli 因服务器 nginx 未编译该模块，**跳过**（如需可后续用宝塔插件方案）

**构建/部署流程**：局域网构建机（`192.168.3.31:2375`，Docker Desktop）`docker build`（约 1.5 分钟，含 i18n 校验 + vue-tsc + vite build + Go 编译）
→ `docker save`（132MB）→ scp 上传 → `docker load` → compose 切换（备份 `docker-compose.yml.bak-perf-20260912`）→ 容器 healthy

**验证结果（全部通过）**：
- 静态资源响应头：`cache-control: public, max-age=31536000, immutable`（且已剥离 ETag → 浏览器直接读磁盘缓存，不发校验请求）
- 二次访问（缓存热）页面 10 秒内完成加载（优化前 17.9s → 观察值大幅改善）
- **切出（→ /dashboard）再切回（→ /chat）：1 秒内 iframe 完整显示、零重载**（keep-alive 生效）
- 四站点正常：main 200 / chat 302 / draw 200 / canvas 200

**遗留（未实施，按需再定）**：
1. **CDN 加速静态资源**（唯一能根治"首次 16s"的方案，需 CDN 服务与域名/回源配置）—— 用户已决定暂缓
2. lobe 前端构建分包优化（manualChunks / 按需引入，需重建 lobe 镜像）
3. keep-alive 常驻三个工作台 iframe 会占用额外内存（各约 100-300MB），如遇内存压力可改为仅保活最近一个

---

## 附二：缺陷修复批次（09-12 凌晨，P1-P4，外部 AI 测试报告的核查与修复）

### P1 生图台 CORS 预检失败 ✅ 已修复（nginx）
- 根因：`ai.1canc.com.conf` 仅 `/v1/` 配置 OPTIONS 短路+CORS，`/pgw/` 未覆盖（预检直达后端 403 无 CORS 头）
- 修复：镜像 `/v1/` 块为 `/pgw/`（OPTIONS 204 + CORS 头 + 代理参数完全一致）
  备份：`/root/perf-backup/ai.1canc.com.conf.bak-20260912-031553`
- 验证：`OPTIONS /pgw/v1/models` = **204 + 完整 CORS 头**（原 403）；代理 GET 401 正常（无 key 时后端语义不变）

### P2-1 充值后旧 Key 报"余额不足"（5-8 分钟自愈）❌ 非 bug（测试假阳性）
- 缓存失效机制完整：管理员充值（admin_user.go ×6）/ 兑换码（redeem_service.go:571,583,590）/
  联盟提成（affiliate_service.go:482）均调用 `InvalidateAuthCacheByUserID`；另有 outbox 兜底（500ms 轮询 + 30s 安全二段）
- 测试为**直接改数据库**绕过应用层 → 不触发失效 → 等 auth 缓存 TTL 自然过期（即 5-8 分钟）
- **注意**：今后手工调整余额请走管理后台；直接 SQL 改库不触发缓存失效

### P2-2 首次进入卡 LobeHub 启动页 ✅ 已修复（lobe v3）
- 根因：bridge-login 注册后立即 302，业务 `users` 行由 better-auth databaseHooks 异步同步，
  首页首个 tRPC 先于同步完成 → `User not found` → 前端卡启动页（刷新自愈）
- 修复：注册成功后**轮询确认业务行就绪**（10×200ms）再 302；注册冲突（并发竞态）时重试一次登录
- 日志佐证：已存在用户（sub2api-2）也偶发 User not found —— 同为业务表同步竞态，本修复覆盖

### P3 默认模型禁用 + 内置模型混入 ✅ 已修复（lobe v3）
- 根因：`bridge.ts` 注入循环对未注入槽位 `continue` 跳过 → anthropic/google 等内置示例模型混入选择器；
  lobe 新用户默认模型 deepseek-v4-flash 被禁用 → "当前模型已禁用"
- 修复：未注入槽位用 `batchUpdateAiModels`（服务端 upsert 语义，可创建 enabled=false 记录）+
  `batchToggleAiModels` 全量下架
- 验证：DB `anthropic 8 条 enabled=false`、`google 5 条 enabled=false`；
  模型选择器**仅显示工作台注入的模型**（ZN-GPT福利组 5 个）✅
- 追加修复（同日第二轮，commit `ce969cee9a`，镜像 v5）：
  - **首页「当前模型」实际读取 inbox agent 的 model**（DB `agents` 表），写入 `defaultAgent` 设置不影响该 UI ——
    补充 `updateAgentConfigById(inboxAgentId, {model, provider})`（与 defaultAgent 设置**双写**）→
    UI 直接显示注入模型 ✅（"当前模型已禁用"提示消失，实测选择器显示 `ZN-GPT福利组 · ZN-gpt-5.6-sol`）
  - **隐藏首页「上新」模型卡片**：卡片展示非注入的内置模型（GLM-5.2/Kimi 等），工作台无意义 ——
    `Home/index.tsx` 移除 `showNewModelShortcuts`
- **遗留（用户报告的关联 bug，未深挖）**：点击被禁模型的「启用」按钮后，切出再切回会话丢失（iframe 内跳转登录页）。
  推测链路：「启用」触发的 tRPC 请求鉴权失败 → 前端全局 401 处理执行 signOut。
  默认模型自动指派上线后，正常流程不再出现「启用」按钮；如需根治需排查该请求的 401 原因

### P4 各项
| 项 | 处理 |
|---|---|
| `already exists` 125 次/4h（CONFLICT 日志刷屏） | ✅ 修复：bridge create 前预检（getAiProviderModelList），已存在直接 update；重启后 CONFLICT 日志 = **0** |
| 画布 default:: 残留（video/audio） | ✅ 修复：画布桥 video/audio 重指派（无注入能力模型则置空）；commit `a0d0570`；静态站已更新（备份 `/root/canvas-backup/canvas-site-20260912-032239.tar.gz`） |
| 价格标签 `"0. 8"`、flare/sunburst 标签为空 | 数据项：后台「工作台配置」手改 |
| 生图尺寸不校验、未知模型 30s+ 才 404 | 未处理：需单独排期梳理 gateway 校验链路 |
| nginx 历史 error（draw 上游拒绝、.well-known 扫描） | 无害，历史已自愈 |

**构建/部署**：`lobe-chat:bridge-sub2api-v3`（构建机 1.5 分钟；commit `39e10651d4`；**v2 保留回滚**）；
画布静态站 `index-DiNKA7Sq.js`（commit `a0d0570`）。

**验证**：anthropic/google 全部下架 ✅；CONFLICT 日志 0 ✅；User not found 0 ✅；四站点正常 ✅

### 遗留（本批次）
1. lobe 新用户默认模型自动指派（当前模型被禁用的提示需用户手点一次）
2. 生图接口入参校验（尺寸白名单、未知模型快速失败）

---

## 附三：未登录首页跳转登录页（09-12 凌晨）

**需求**：未登录访问主站首页（`/`、`/home`）不再显示落地页，直接到 `/login` 登录注册页。

**实现**：`frontend/src/router/index.ts` 守卫 `!requiresAuth` 分支增加
未登录 + `/home` → `next('/login')`；模型广场禁用时的未登录兜底同步改为 `/login`。
（`/` 的 redirect 保持 `/home` —— 由守卫统一拦截；已登录用户访问 `/home` 行为不变）

**部署**：镜像 `sub2api:home-login-20260912`（commit `ff2663b03`；构建机 1.5 分钟；
compose 备份 `docker-compose.yml.bak-home-login-20260912`；上一版 `perf-20260912` 保留回滚）

**验证**：未登录 `/` → /login ✅、未登录 `/home` → /login ✅、已登录 `/home` 正常显示 ✅

---

## 附四：对话工作台 C 端轻量化裁剪（09-12，lobe fork v6）

**需求**：面向 C 端（AI 推文用户）把 lobe 对话工作台裁到只剩「对话 + 话题 + 资源」，
去掉主页面、助理页、搜索、智能模式、记忆、技能、生成等全部外围功能；助理锁死、无感登录、进站直达对话。

**最终形态**：
```
侧栏 [默认助理名(锁死)] → ⚐新建话题 / 💬话题 / 📦资源（下方话题列表）
对话区：消息流 + 输入框（模式固定"对话"，+号仅剩 上传/联网搜索(默认关)/模型参数）
```

**改动（lobe-chat 仓库 `E:\github\sub2AI\lobe-chat`，canary 分支，基线 tag `pre-trim-v5`）**：

| 层 | 动作 |
|---|---|
| 路由 | 物理删 `(main)` 下 18 目录（home/agents/apps/community/memory/settings/task/page/eval/(create) 等）+ onboarding + desktop-onboarding + agent 子页（profile/share/channel/statistics/docs/goal*/permission/self-learning/task*/topics 保留）+ mobile/community + popup/group |
| 路由总表 | 重写 `desktopRouter.shared.tsx` / `desktopRouter.config.tsx` / `mobileRouter.config.tsx`：仅 agent(对话+topics) + resource；根路径与 catch-all 统一 `redirectElement('/agent')` |
| 侧栏 | `NavPanel/Shell.tsx` 移除①全局侧栏 HomeSidebar；`AgentSidebar/Header/Nav.tsx` 三件套；`SwitchPanel` 锁死纯展示 |
| 输入框 | `ControlBar/index.tsx` 删模式切换器；`useEffectiveAgentMode` 强制 chat；`Plus` 删记忆/技能项（联网搜索保留）；删 `ActionBar/Tools` 目录 |
| 顶部 | 删 TerminalPanelToggle / WorkingPanelToggle / ShareButton + "..." 菜单宽屏开关 |
| 登录 | `useUserStateRedirect.ts` 中性化 onboarding 跳转（无感登录直达对话） |
| 默认助理 | `packages/const/src/settings/agent.ts` 预置推文人设 systemRole + `searchMode:'off'` + `enableAgentMode:false` |
| 依赖兼容 | 路由注册表保留全量（防 `getRouteById` 非空断言崩溃）；`agent/channel/const.ts` 存根 `getPlatformIcon`；`useInterceptingRoutes` 打开助理设定置 no-op |
| 死簇清理 | 删 SkillStore/AgentSkillStore/MCPPluginDetail/ProfileEditor/AgentShareSettings/AgentProfileTabs/AgentUsage 等 feature 目录 |

**构建/部署**：本地改码 → 构建机（`tcp://192.168.3.31:2375`）`lobe_build_v6.cmd`（5 轮收敛）→
`docker save`（tar 1.06GB，镜像 2.2GB，较 v5 2.25GB 略小）→ scp → `docker load` → compose image 切 `bridge-sub2api-v6`（备份 `docker-compose.yml.bak-v6-20260912`）→ `up -d lobehub`。

**验证（服务器侧）**：容器 0 重启、Gateway 启动成功；四站点全 200；chat /agent /resource 302（未登录跳登录，正常）；磁盘 43%。

**待办/注意**：
1. **需浏览器实测**（bridge-login 登录后）：对话流式、话题持久化、资源上传、模型注入、助理锁死、联网搜索默认关、直达 /agent 无 onboarding
2. 本地 lobe-chat 改动未 git 提交（commit 前需确认）；回滚点：`pre-trim-v5` tag + 服务器 v3/v4/v5 镜像
3. 服务器 `/opt/playground/src/lobe-chat` 为旧参考副本，与本地 canary 已不同步，注意区分

---

## 附五：白屏 / 偶发跳登录修复 + 记忆后台链路关闭 + 渠道监控状态标签（09-12 白天）

### 1. 白屏（重定向死循环）
**现象**：登录后对话页白屏。**根因**：`/agent` 空路径原设计 `redirect('..')` 跳回首页，由首页再引导进 inbox；首页删除后根路径 `/` 又改跳 `/agent`，形成 `/ → /agent → / → …` 死循环。
**修复**：`desktopRouter/mobileRouter` 的 `/agent` 空路径 → `inbox`，根路径与 catch-all → `/agent/inbox`。无头浏览器实测渲染正常。

### 2. 偶发跳 lobe 登录界面
**根因**：`lobe_ticket` 5 分钟有效，壳侧"重新加载"/分组切换会用**同一个过期票据**重载 iframe → `verifyTicket` 失败 → `302 /signin`。
**修复**：`bridge-login/route.ts` 在验票前先查已有会话，有有效会话直接回 `callbackUrl`（票据定位为首次引导的一次性凭证）。实测无效票据+有效会话 → 保持登录。

### 3. 记忆后台链路关闭
**现象**：日志持续刷 `Failed to retrieve memory for topic ... text-embedding-3-small ... model_not_found`。
**修复**：`apps/server/.../userMemories.ts` 的 `retrieveMemoryForTopic` 加生产可用总开关 `DISABLE_AUTO_MEMORY=1`；`docker-compose.override.yml` 追加 `DISABLE_AUTO_MEMORY=1`。lobe v6 重建后生效（容器 env 已确认 SET）。

### 4. 渠道监控状态标签（新功能）
**需求**：工作台配置里每个模型可关联一个渠道监控；对话/生图/画布里显示模型时带渠道状态标签。
**实现**：
- 迁移 `240_playground_app_models_monitor.sql`：`playground_app_models.monitor_id`（软引用 channel_monitors，ON DELETE SET NULL）
- 后端：repo/service/handler 透传 `monitor_id`；`ChannelMonitorService.PlaygroundStatusIndex`（门控 Enabled+mode=v1）+ `ResolveMonitorModelStatus`（主模型→附加模型→主模型兜底）；用户端 `playground-config` 下发 `monitor_status`
- 管理端：工作台配置页模型行加「渠道监控」下拉（复用 `channelMonitorAPI.list`）
- 壳侧：`withMonitorStatusTag` 把状态 bake 进 `display_name`（🟢可用/🟡降级/🔴异常），三个工作台渲染模型名处统一显示（生图 `modelPicker`、画布 `displayName`、lobe `displayName` 均已确认消费 display_name）
**部署**：`sub2api:monitor-tag-20260912`（compose 备份 `.bak-monitor-20260912`）；迁移已应用（`monitor_id` 列存在）。状态为下发时快照，监控功能关闭/非 v1 模式时不展示标签。

---

## 附六：生图工作台 Agent 优化 + 画布指纹修复 + 工作台新标签页（09-12 下午）

> 承接附五，本轮处理用户反馈的三个体验问题；三个仓库的代码已提交（sub2api `73157d84c`、gpt_image_playground `dec25e3`、infinite-canvas `0e9935f`）。

### 1. 生图工作台：Agent 模式双模型选择器 + 布局优化

**问题**：① Agent 模式下无法选择运行智能体的 LLM（配置的文本模型没有 UI 入口）；② 两个模型选择框并列后把右侧尺寸/质量等参数挤成一团；③ 下拉过窄致长模型名截断；④ 只有第一个模型有两行价格展示。

**实现**（`gpt_image_playground`）：
| 文件 | 改动 |
|---|---|
| `InputBar.tsx` | Agent 模式并列「智能体模型（文本 profile）/ 生图模型（图像 profile）」并加中文标签；输入栏容器按模式放宽（Agent `max-w-7xl` 1280px / 画廊 `max-w-5xl` 1024px） |
| `Select.tsx` | 下拉面板宽度自适应内容（`w-max min-w-full max-w-[min(92vw,460px)]`） |
| `modelPicker.tsx` | 新增 `label/models/value` props；无 `price_label/unit_hint` 时从展示名末尾括号提取价格作副标题，保证所有模型两行展示 |

**部署**：`/var/www/image-app`（入口 `index-CWaL1bqD.js`；备份 `/root/image-app-backup/image-app-site-20260912-144908.tar.gz`）

**实测**：Agent 模式双选择器 + 6 参数均匀展开，参数区每列 111px（原约 58px），控件文本完整；画廊容器同步放宽至 1024px。

### 2. 无限画布：bridge 幂等指纹修复

**现象**：后台调整模型价格或渠道监控状态变化后，画布模型选择器一直显示旧数据（模型名残留「🟢 可用」、不显示价格）。

**根因**：`infinite-canvas/web/src/lib/sub2api-bridge.ts` 的幂等指纹仅含模型 ID（`m.name`），展示元数据变化被判定「未变化」直接短路，不再写入渠道。

**修复**：指纹纳入 `displayName/priceLabel/unitHint`。

**部署**：画布静态站（入口 `index-QzIP92sU.js`；备份 `/root/canvas-backup/canvas-site-20260912-143418.tar.gz`）

**验证**：postMessage 注入带价格配置 → 画布 store 正确落库 `displayName/priceLabel/unitHint`。

**注意**：画布（canvas app）的 8 个模型当前**未配置价格**（DB `price_label` 为空），需在后台「工作台配置 → 无限画布」填写后即可显示（代码链路已就绪）。

### 3. sub2api：配置刷新增强 + 新标签页

**智能体模型只显示「可用」的修复**：
- 根因：LLM 模型未配置展示名（`display_name` 空），且生图工作台为 URL 参数注入模式，keep-alive 下 iframe 不重建 → 页面内一直是旧配置
- 修复：`PlaygroundView.vue` 的 `onActivated` 静默刷新配置并重注入 —— postMessage 模式原地重发；**URL 模式用配置指纹比较，变化时重建 iframe**
- 附带：`withMonitorStatusTag(m.display_name || m.model_id, ...)`，未配置展示名时以 model_id 兜底

**新标签页**：侧边栏「对话 / 生图工作台 / 无限画布」三个入口加 `target="_blank"` + `rel="noopener"`（管理员「我的账户」区块同组菜单同步生效）。

**提交**：`73157d84c`；**部署**：`sub2api:newtab-20260912`（compose 备份 `.bak-newtab-20260912`）

### 4. 本批部署汇总（回滚点）

| 服务 | 版本 | 回滚点 |
|---|---|---|
| sub2api 主站 | `sub2api:newtab-20260912` | `.bak-newtab-20260912`；上一版 `lobe-status-20260912` 镜像保留 |
| 生图工作台 | `index-CWaL1bqD.js` | `/root/image-app-backup/image-app-site-20260912-144908.tar.gz` |
| 无限画布 | `index-QzIP92sU.js` | `/root/canvas-backup/canvas-site-20260912-143418.tar.gz` |
| 对话工作台 lobe | `bridge-sub2api-v6` | 未变动 |

### 5. 遗留 / 注意
1. **画布价格需配置**：canvas app 的 8 个模型 `price_label` 为空（chat 已配 `0. 8`，疑似笔误，建议改 `0.8` 并补单位）
2. 生图工作台在后台配置变化时会自动重建一次 iframe（URL 注入模式的必要行为），未变更时不受影响
3. 三个仓库本地提交均未推送远端
### 6. 服务器清理（09-12 下午）

**扫描（清理前）**：磁盘 43G/79G（57%）；Docker 镜像 26 个 / 33.45GB（可回收 28.14GB）

**已执行（安全级，共释放约 20GB）**：
| 项 | 大小 | 说明 |
|---|---|---|
| 悬空镜像（8 个） | 18.35GB | lobe v3~v6 构建中间层（无标签、无容器引用） |
| `/opt/sub2api-build/lobe-v4.tar` + `lobe-v5.tar` | 2.2GB | 镜像已 load 进 Docker，服务器 tar 冗余 |
| `/tmp/a_*.json`（29 个） | 1.5MB | **含渠道账号凭证的敏感导出**，必须删 |
| `/tmp` 调试残留 40+ 个（json/html/key/token/脚本） | ~2.5MB | 含 2 个 admin JWT、5 个 API Key（sk-*）文件、页面抓取残留 |

**结果**：磁盘 43G → **24G（57% → 31%）**；/tmp 1.5M+残留 → **56K**（仅系统 socket / 宝塔 / systemd 文件）

**建议后续处理（待确认，约 5.5GB）**：
| 项 | 大小 | 说明 |
|---|---|---|
| 旧 sub2api 镜像（`local-20260911-2` / `perf-20260912` / `home-login-20260912`） | ~850MB | 保留 `monitor-tag` / `lobe-status` / `newtab` 即可 |
| `lobe-chat:v2` + `v3` | ~4.5GB | 保留 v5（最近回滚点）与 v6（当前） |
| journal 日志 | 32M → 10M | `journalctl --vacuum-size=10M` |
| apt 缓存 | 79M | `apt clean` |
| `/root/image-app-backup-20260912-125621.tar.gz` | 1.4MB | 已被目录形式备份取代 |

**不能删（保留）**：8 个运行中容器镜像、Docker 数据卷（postgres/redis/minio）、站点文件（`/www/wwwroot/*`、`/var/www/image-app`）、nginx vhost 配置（含 s3 特殊配置）、compose 与 .env、`/root/sub2api-deploy/data`、近期回滚镜像与备份、`/opt/playground/src`（lobe 源码）、`/www/backup`（宝塔）、MinIO `lobe-temp` 数据。

**⚠️ 安全提醒**：清理中发现并删除多个含凭证的临时文件（2 个 admin JWT、5 个 sub2api API Key `sk-*`、渠道账号导出 JSON）；建议确认这些测试 key 是否需在后台吊销。