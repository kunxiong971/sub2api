# 运维与项目进度记录 — 2026-09-11

> 记录范围：AI 聚合平台（sub2api 主站 + 集成工作台三应用）当日部署、验证与运维动作。
> 环境：生产服务器（`ai.1canc.com` 实例，宝塔 nginx 为唯一公网入口）。

## 一、当前运行拓扑（本日收尾状态）

| 服务 | 镜像 | 监听 | 状态 |
|---|---|---|---|
| sub2api 主站 | `sub2api:local-20260911-1703`（= `sub2api:latest`） | 127.0.0.1:8080 | healthy |
| 对话工作台 lobehub | `lobe-chat:bridge-sub2api-v2` | 127.0.0.1:3210 | Up |
| 生图工作台 image-app | `nginx:1.27-alpine` + `/var/www/image-app` | 127.0.0.1:3002 | Up |
| 无限画布 canvas | `local/infinite-canvas:latest` + 静态站 | 127.0.0.1:3000 / 静态目录 | Up |
| 数据库 | sub2api-postgres / sub2api-redis / lobehub-postgres | 仅内部网络 | healthy |

域名：主站 `ai.1canc.com`；工作台 `chat / draw / canvas .ai.1canc.com`（宝塔反代/静态站）。

## 二、今日变更

### 1. sub2api 镜像更新（本地构建版上线）

- 新版本：`sub2api:local-20260911-1703`（替换 `custom-20260911-5`）
- 构建方式：本地局域网构建机（Docker Desktop）构建 → `docker save` 导出 tar → scp 上传 → `docker load`
- 完整性校验：tar 字节数一致；容器内二进制 SHA256 与本地构建产物一致
- 配置切换：`/root/sub2api-deploy/docker-compose.yml` image 行已更新（备份 `.bak-pre-switch-20260911`）
- 回滚备份：`.bak-local-deploy-20260911`（compose + .env）

### 2. lobe-chat 重建部署（隐藏自带模型补丁）

- 新版本：`lobe-chat:bridge-sub2api-v2`（替换 `bridge-sub2api`）
- 补丁内容：`src/sub2api/bridge.ts` 新增逻辑——注入完成后调用 `batchToggleAiModels` 禁用注入槽位上 lobe 内置的默认模型
- 构建方式：本地构建机构建（约 6 分钟）→ 导出 tar（约 1.05GB）→ 上传 → `docker load` → compose 切换
- 效果：对话模型选择器中原生自带的 **DeepSeek V4 Flash / V4 Pro / Flash Vision Exp 已不再显示**，仅保留工作台配置的模型

### 3. 服务器磁盘清理

- 原因：lobe-chat 反复构建（约 5-6 轮）产生大量中间残留，磁盘占用由 ~17G 膨胀至 60G
- 清理内容：
  - 6 个已退出的构建临时容器（约 8.1G）
  - 8 个悬空（dangling）镜像（各轮构建的中间层）
  - 2 个旧版回滚镜像（`lobe-chat:bridge-sub2api` / `sub2api:custom-20260911-5`）
  - 上传的镜像 tar 包、旧源码包，apt 缓存、journal 日志、历史 bun 运行时等
- 结果：**磁盘已用 60G → 21G（可用 17G → 56G），释放约 39G**；`/var/lib/containerd` 48G → 12G

## 三、验证记录（全部通过）

- 主站 200；工作台壳页面 `/chat` `/image` `/canvas` 均 200
- CORS 预检 204；CSP `frame-src` 含三个工作台子域；免登录桥 `bridge-login` 302 → 会话建立正常
- 浏览器实测：管理后台「工作台配置」页、三个工作台 iframe（对话/生图/画布）渲染正常，console 无错误
- 对话工作台：模型选择器仅显示工作台配置的模型（内置 DeepSeek 模型已隐藏）
- 数据库每日备份正常（`/opt/backups`，pg_dumpall，保留 7 天）

## 四、待办 / 注意事项

1. 对话工作台其余品牌槽位的免费模型（Claude / Gemini 等）如需一并隐藏，可扩展 bridge 补丁（禁用非注入槽位的内置模型）
2. 本地构建机构建前需确保基础镜像可用（`node:24-slim`、`busybox`；本机镜像源已配置 daocloud 拉取方式）
3. 服务器已无 lobe/sub2api 构建中间层残留；后续重建建议仍走「本地构建机导出镜像 + 上传加载」方式，避免服务器内存不足导致构建失败
4. 工作台前端存在多套历史构建残留（`dist` 未清理叠加所致），下次前端构建前建议先清空 `backend/internal/web/dist`

## 五、回滚说明

- 本日清理已删除服务器端旧的回滚镜像，如需回滚：
  - sub2api：本地保留 `sub2api-image.tar`（本次部署所用版本），可重新上传 `docker load`
  - lobe-chat：本地保留 `lobe-chat-v2.tar`（即当前版本）；历史版本可按需从源码重建（本地构建机约 6 分钟）
- 配置回滚点：
  - `/root/sub2api-deploy/docker-compose.yml.bak-pre-switch-20260911`
  - `/opt/playground/docker-compose.yml.bak-pre-lobe-v2-switch`
