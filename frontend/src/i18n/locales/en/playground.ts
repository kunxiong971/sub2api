/**
 * Playground (workbench shell page) user-facing messages
 * fork: separate from the admin-side admin.playground namespace
 */
export default {
  playground: {
    update: {
      available: 'A new version is available. Refresh to update.',
      reload: 'Refresh',
      dismiss: 'Later'
    }
  }
}
